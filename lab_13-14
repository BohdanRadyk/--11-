1)
#include <stdio.h>
#include <math.h>

double geron(double a, double b, double c) {
    double p = (a + b + c) / 2;
    return sqrt(p * (p - a) * (p - b) * (p - c));
}

int main() {
    double u, v, w;
    double s;

    printf("Введіть сторони трикутника: ");
    scanf("%lf %lf %lf", &u, &v, &w);
    
    s = geron(u, v, w);
    printf("Площа 1 трикутника: %.2lf\n", s);
    
    s = geron(10.3, 8.1, 9.7);
    printf("Площа 2 трикутника: %.2lf\n", s);

    s = geron(u + 10.3, v + w, w * 1.7);
    printf("Площа 3 трикутника: %.2lf\n", s);

    return 0;
}
2)
#include<stdio.h> 
#include<math.h>
double fract(int,int); 
double factorial(int); 
int main( ) {
int m,n,s;
scanf ("%d %d",&m,&n); 
printf ("%lf\n",fract(m+1,n+1));
return 0;
}
double fract(int x, int y) { double t; 
t=factorial(x+y)/(x*y); return t;
}
double factorial (int n) { int i;
double p;
p=1;
for(i=1;i<=n;++i) p*=i;
return p;
}
3)
#include <stdio.h>
#include <math.h>

double fract(int x, int y) {
    double t;
    t = (double)(x % y) / y;
    return t;
}

int main() {
    int m, n;
    scanf("%d %d", &m, &n);
    printf("%lf\n", fract(m + 1, n + 1));
    return 0;
}




